<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hci_bank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgotPassword"></activity>
        <activity android:name=".Settings" />
        <activity android:name=".Cards" />
        <activity android:name=".PassbookSummary" />
        <activity android:name=".PassbookStatements" />
        <activity android:name=".BenificiaryDetails" />
        <activity android:name=".MoneyTransfer" />
        <activity android:name=".Main" />
        <activity android:name=".MainActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>